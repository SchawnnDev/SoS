#ifndef SOS_HEADER_ASM_H
#define SOS_HEADER_ASM_H
#include "asm.h"

/**
 *  Write the header of the assembly file:
 *  global variables and error messages to print
 *
 * @return 0 if success, -1 if it fails
 */
int asm_writeHeader();

#endif //SOS_HEADER_ASM_H
