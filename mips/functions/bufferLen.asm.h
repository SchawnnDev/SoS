#ifndef SOS_BUFFERLEN_ASM_H
#define SOS_BUFFERLEN_ASM_H
#include "asm.h"

/**
 * Counts the number of characters in a
 * buffer until \0 is encountered.
 *
 * @return 0 if success, -1 if it fails
 */
int asm_writeBufferLenFunction();

#endif //SOS_BUFFERLEN_ASM_H
