if test 50 -ne 50
    then echo "Shouldn't print that"
elif test 50 -eq 50
    then echo "Should print that"
fi;
echo "\n";

if test 50 -ne 50
    then echo "Shouldn't print that"
elif test 80 -eq 50
    then echo "Shouldn't print that"
else
    echo "Should print that"
fi;
exit